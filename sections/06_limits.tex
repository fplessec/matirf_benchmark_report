\section{Intrinsic limits}
\label{sec:limits}

% Distilled from docs/algorithms/limits.md (Etape 3). One line: on MA-TIRF every wall is the
% same wall -- the ~47-direction null space -- and the only lever is the prior.
% TABLE: algorithm | intrinsic wall | failure signature | can it be moved?
% Optional FIGURE: one failure mode (ADMM emptying the image; collapse under noise).
