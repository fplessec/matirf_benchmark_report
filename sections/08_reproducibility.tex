\section{Reproducibility}
\label{sec:repro}

% Every figure/number is regenerable. TODO:
% - Each showcase reconstruction is stored in results/<figure_id>/ as config.toml (the exact
%   run) + f.tif (the reconstruction) [+ truth.tif when synthetic]. Re-running matirf_python_plus
%   on that config.toml reproduces f.tif.
% - The full campaign lives in matirf_python_plus/benchmarks (campaign.py + runner.py); how to
%   launch/resume/stop it.
% - Code version: the v2.0 tag of matirf_python_plus.
